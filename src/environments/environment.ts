// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCjCZPazXGMvAnEyIsrgciLy7IVOqPT4zk',
    authDomain: 'clip-service-e3f93.firebaseapp.com',
    projectId: 'clip-service-e3f93',
    storageBucket: 'clip-service-e3f93.appspot.com',
    messagingSenderId: '542309958079',
    appId: '1:542309958079:web:a39dca00de2cb0dc5ce10a'
  }
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
